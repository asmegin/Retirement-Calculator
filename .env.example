# Copy to .env for Docker Compose. A direct Node launch needs process variables.
APP_VERSION=1.0.0
HOST_PORT=3333

# Set both credentials before enabling authentication. Do not commit .env.
BASIC_AUTH_ENABLED=false
BASIC_AUTH_USER=
BASIC_AUTH_PASS=
